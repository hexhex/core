% The program is safe because testSubstr returns only smaller strings wrt. to the wellordering of string length

%s("helloworld").
%s(Y) :- s(X), &testSubstr[X,1,10](Y).

s(a).
s(Y) :- s(X), &testConcat[X, a](Y), &testStrlen[Y](Len), &testSmallerThan[Len, 10]().
